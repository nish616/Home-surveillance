services:
  detection:
    image: detection-service
    ports:
      - "8000:8000"
    environment:
      ACCESS_TOKEN: ${ACCESS_TOKEN}
      RTSP_URL: ${RTSP_URL}
    volumes:
      - ./snapshots:/app/snapshots
      - ./events:/app/events
    restart: unless-stopped
